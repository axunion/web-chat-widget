{
  "name": "web-chat-widget-backend-example",
  "private": true,
  "type": "module",
  "description": "Reference proxy backend for web-chat-widget. Not part of the published package.",
  "scripts": {
    "start": "node --env-file=.env server.ts",
    "dev": "node --env-file=.env --watch server.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  },
  "volta": {
    "node": "24.15.0"
  }
}
